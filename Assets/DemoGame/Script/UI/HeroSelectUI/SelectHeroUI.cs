using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace DC.UI
{
    public class SelectHeroUI : BaseUI
    {
        private SelectHeroUIViewGen mView;

        public override void Init(params object[] param)
        {
            base.Init(param);

        }
    }
}
